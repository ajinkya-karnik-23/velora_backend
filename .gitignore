.venv/
venv/
.env
.env.bak*
.env.*.bak
*.pyc
__pycache__/
storage/

.vscode/*
!.vscode/launch.json

# Ignore local Node dependencies and generated builds inside the engine
.quartz-engine/node_modules/
.quartz-engine/public/
.quartz-engine/.next/

# Ignore the local shortcut link we made
.quartz-engine/content

#KB

# Client-supplied data — control definitions, evidence and report templates.
# Never committed: it is per-deployment and located via CLIENT_DATA_PATH.
# See .env.example for the expected folder layout.
Arcelor-Mittal/
client-data/
