.venv/
venv/
.env
.env.bak*
.env.*.bak
*.pyc
__pycache__/
storage/

.vscode/*
!.vscode/launch.json

# Ignore local Node dependencies and generated builds inside the engine
.quartz-engine/node_modules/
.quartz-engine/public/
.quartz-engine/.next/

# Ignore the local shortcut link we made
.quartz-engine/content

#KB

# Client data lives in poc-2-data/ and IS tracked on this branch so the POC
# can be cloned and run as-is. CLIENT_DATA_PATH still points at it, so a
# different deployment can relocate it without code changes.
client-data/

# macOS
.DS_Store
